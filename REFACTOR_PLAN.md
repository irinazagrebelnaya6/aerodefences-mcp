# Refactor Plan — AeroDefences MCP Server

План усунення 6 зауважень код-рев'ю щодо стилю та продуктивності. Це **не баги**,
а полірування, тож головна умова — **поведінка й
публічний API лишаються незмінними**:

- 32 інструменти + 1 ресурс (`resource://schema`) + 1 prompt (`compliance_report`);
- точка входу `server_aerodefences.py` і ре-експорти `mcp` / `query` / `METRICS`
  (їх використовують тести й харнеси);
- ті самі результати інструментів.

Запобіжник після **кожної** фази: `pytest -q` (11 тестів) + `ruff check .` +
звірка інвентарю (32/1/1) + завантаження stdio-підпроцесом.

Легенда: ✅ зроблено · 🚧 у роботі · ⬜ заплановано.

> **Статус: усі фази A–D виконано.** Усі 6 зауважень закрито; поведінка й
> публічний API незмінні (32/1/1, 11/11 тестів, ruff чистий).

---

## Зауваження → рішення

| # | Зауваження | Рішення | Фаза | Ризик | Статус |
|---|-----------|---------|------|-------|--------|
| 3 | `get_product` завелика (5 таблиць в одній функції) | Витягти `_load_specs/_features/_faqs/_images/_use_cases` | A | низький | ✅ |
| 4 | Послідовні `await query()` (повільно) | `asyncio.gather` для НЕЗАЛЕЖНИХ запитів (`get_product`, `catalog_stats`) | A | середній | ✅ |
| 1 | Забагато глобалів (`POOL`, `DB_CONFIG`, `TRANSPORT`, `METRICS`, `ROLES`, …) | `@dataclass Config`, `class Database`, `class Metrics` + синглтони; зберегти module-alias `query`/`METRICS` | B | середній | ✅ |
| 6 | Повтор SQL (`SELECT … FROM products WHERE …`) | Спільні шматки (колонки, `by_slug`) — вбирається Repository | C | середній | ✅ |
| 2 | Повтор `query()` у кожному інструменті | `ProductRepository` / `CategoryRepository` / `FaqRepository` | C | вищий | ✅ |
| 5 | Немає кешу | Кешувати **лише** `resource://schema` (реально статична) | D | середній | ✅ |

---

## ⚠️ Застереження, зафіксовані до старту

- **Кеш категорій — НЕ наївно.** `list_categories` рахує `COUNT(products)`, який
  змінюється після write (напр. `bulk_set_status`). Наївний кеш дав би застарілі
  цифри. Кешуємо тільки схему БД; категорії — або пропускаємо, або короткий TTL з
  інвалідацією в `_run_write` (окремо, обережно). За замовчуванням — пропускаємо.
- **`asyncio.gather` і пул.** `get_product` дає рівно 5 паралельних дозапитів; пул
  `maxconnections=5` (дефолт) це витримує. Якщо піднімати паралелізм деінде —
  звірити з `ADD_DB_POOL_SIZE`.
- **`_run_write` НЕ паралелимо.** Тільки незалежні READ ідуть у `gather`.
- **Сумісність тестів.** Будь-яке загортання `query`/`METRICS` у класи має зберегти
  module-рівневі імена (`query`, `METRICS`), інакше `tests/` і харнеси зламаються.

---

## Фази

### ✅ Фаза A — `get_product` (п.3 + п.4) · низький ризик, висока віддача
- Розбити `get_product` (у `ad_tools_read.py`) на приватні `_load_specs`,
  `_load_features`, `_load_use_cases`, `_load_faqs`, `_load_images`.
- Обгорнути 5 незалежних дозапитів у `asyncio.gather` → картка товару збирається
  паралельно (результат ідентичний).
- Те саме `gather` для 3 незалежних агрегатів у `catalog_stats`.
- **Verify:** `get_product` повертає ті самі ключі/значення; тест
  `test_update_stock_roundtrip` (читає картку) зелений.

### ✅ Фаза B — групування глобалів (п.1) · середній ризик
- `Config` (frozen dataclass з `ADD_*`), `Metrics` (клас навколо лічильників +
  Prometheus), `Database` (пул + `query`/`run_write`) — у відповідних `ad_*`.
- Синглтони `config`, `metrics`, `db`; **зберегти** `query = db.query`,
  `METRICS = metrics.data` як module-рівневі аліаси.
- **Verify:** `srv.query`, `srv.METRICS` доступні й поводяться як раніше; RBAC-тести
  (`writes_denied`) і round-trip (`writes_committed`) зелені.

### ✅ Фаза C — Repository (п.2 + п.6) · вищий ризик, дрібними комітами
- `ProductRepository` (`get_by_slug`, `list_published`, `search`, `by_category`…),
  `CategoryRepository`, `FaqRepository` — інкапсулюють SQL; спільні колонки/`WHERE`
  живуть тут (закриває п.6).
- Інструменти викликають репозиторій замість «сирого» `query`.
- Робити по одному репозиторію за коміт, `pytest` після кожного.
- **Verify:** інвентар 32/1/1 без змін; усі read/write-тести зелені.

### ✅ Фаза D — кеш схеми (п.5) · середній ризик
- Кеш `resource://schema` на час життя процесу (значення будується раз).
- Категорії свідомо БЕЗ кешу (див. застереження).
- **Verify:** `resource://schema` повертає той самий JSON; повторний виклик не б'є в БД.

---

## Поза планом (свідомо)
- Юніт-тести на `get_product`/repository — за бажанням додати у Фазі A/C, щоб
  підсилити тонке поточне покриття (не обов'язково для здачі).

## Готовність до захисту
Кожен пункт має відповідь на можливе питання викладача: чому саме так зроблено
(див. таблицю) і чому частина винесена в план, а не втілена одразу (ризик регресій
проти навчального дедлайну). План дозволяє показати розуміння принципів SOLID/DRY
навіть якщо не всі фази виконано.
